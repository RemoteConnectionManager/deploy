FIND_PACKAGE(LAPACK)
IF(LAPACK_FOUND)
	MESSAGE("LAPACK_LIBRARIES-->${LAPACK_LIBRARIES}<--")
	MESSAGE("LAPACK_LINKER_FLAGS-->${LAPACK_LINKER_FLAGS}<--")
ELSE()
	set(Package_current_dependencies LAPACK)
	if(UNIX)
		set(LAPACK_LINKER_FLAGS "-L${EXTERNAL_ASSEMBLY_COMMON_PREFIX}/lib\ -llapack\ -lblas")
	endif()
ENDIF()